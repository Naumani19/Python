#q4


#q2
#Write a function swap(s) that takes a string as an argument and swaps every-other character 
#Example swap('hello world')
#ehll oowlrd  
    
def swap2(s):
  odds = ""
  evens = ""
  c = list(s)
  for index in c:
    s[index], s[index+1] = s[index + 1], s[index] 
    ''.join.(c)
    
    #  evens = evens + s[index]
    #if not (index %2 == 0):
     #  odds = odds + s[index]
    #pile = evens + odds
    #print pile  
  #print pile
    