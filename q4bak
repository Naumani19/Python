def convert_string(val):
  pile = ""
  for c in val:
    a = ord(c)
    if a%2 == 0:
      pile = pile + '2'
    elif a%3 ==0:
      pile = pile + '3'
    elif a%2==0 and a%3==0:
      pile = pile + '2'
    else:
      pile = pile + '0'
  return pile
    
    